{
  "name": "lego-builder",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "catalog:build": "node scripts/build-ldraw.mjs",
    "dev": "vite --host 0.0.0.0 --port 8098",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 8098",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/main.js && node --check src/scene.js && npm test && npm run build"
  },
  "dependencies": { "three": "0.186.0", "lucide": "^0.577.0" },
  "devDependencies": { "vite": "^7.3.1" }
}
